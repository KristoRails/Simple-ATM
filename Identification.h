#include "headers.h"

bool PinIdentification(int pin, int clientPIN); // Check if the input pin is the same as the one for the user
int InputPin(); // When the user puts their card in, this function is called